# Evidence: P0 — add-region-filter-to-grading

- change: add-region-filter-to-grading
- gateReady: YES
- Scope:
  - 客户分级（order-grading/customer）：
    - type.ts 的 CustomerFilterValues 新增 `saleZoneSum?: string`
    - control-bar/index.vue 新增 saleZoneSumArr computed（逗号字符串 ↔ 数组，参照 saleZonesArr 模式）
    - 模板在「区域部门」field 之前插入「区域」field，Select options 用 `enumStore.customerRegionSumFilterOptions`（数据源来自 enum 的 saleZoneSum）
  - 产品分级（order-grading/product）：同上，ProductFilterValues + control-bar
  - 数据源复用上一轮已实现的 `customerRegionSumFilterOptions`（无需改 enum store）
  - Non-goals（明确不做）:
    - 不改 enum store / API / 路由
    - 不改 en-US 语言包
    - 不改请求参数组装逻辑（useCustomerOrchestration/useProductOrchestration 显式提取字段，新增 saleZoneSum 不影响现有请求）
- Risks:
  1. 筛选生效边界：新增的 saleZoneSum 若未接入 orchestration 的 apiParams，筛选项仅 UI 可见、不参与请求过滤——需用户确认是否仅 UI（改动范围分叉点）
  2. 字段顺序：模板必须在「区域部门」field 之前插入「区域」field，顺序错误则需求不满足
  3. 类型安全：CustomerFilterValues/ProductFilterValues 新增可选字段，需确认无必填性破坏（可选字段不会破坏现有消费方）
