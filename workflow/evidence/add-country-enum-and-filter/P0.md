# Evidence: P0 — add-country-enum-and-filter

- change: add-country-enum-and-filter
- gateReady: YES
- Scope:
  - enum store 新增「国家」枚举：getCountryApi()（GET /app/rest/country）+ countryList 状态/fetchCountryOptions/getter（参照 customerRegionList 模式）
  - 客户分级筛选项：type.ts 加 country、control-bar 加 computed + field（区域部门与系统分级之间）、orchestration 4 处接入（getCommonParams/toPostParams/defaultParams/watch）
  - 客户分级表格：customer-table/types.ts 的 CustomerRow 加 country、use-columns.tsx 在 saleZone 列后加「国家」列
  - 数据源复用 enum 模式；不做 en-US
  - Non-goals（明确不做）:
    - 不改产品分级/其他页面
    - 不改 API 契约字段（country 为新字段）
- Risks:
  1. 后端契约：country 枚举值格式未知（name/code？）——参照 sale-zone 接口假设 {name, code}，若不同需调整 fetchCountryOptions 映射
  2. 表格 country 字段：CustomerRow 新增必填字段，接口返回需包含 country，否则 undefined 渲染 '-'（已兜底）
  3. 筛选项顺序：国家 field 须在区域部门之后（需求：数据表区域部门后加国家列；筛选参照）
  4. watch 遗漏：orchestration 4 处必须齐全，否则筛选不触发请求
