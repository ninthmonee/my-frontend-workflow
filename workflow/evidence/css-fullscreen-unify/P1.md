# P1 — 开发

## Changed Files

| # | 文件 | 改动 |
|---|------|------|
| 1 | `packages/@core/composables/src/use-platform-fullscreen.ts` | 移除 `isIPadOSSafari()` + `@vueuse/core` 依赖；统一为 CSS 全屏；新增本地 `MaybeElementRef` 类型；更新 JSDoc |
| 2 | `apps/main-app/src/views/demand-forecast/forecast-results/components/tab-container/index.vue` | ① `getPopupContainer` 改为返回 `predictContentRef.value \|\| document.body`；② 删除 `:fullscreen` / `:-webkit-full-screen` / `::backdrop` 伪类规则，仅保留 `.predict-content.is-css-fullscreen` |
| 3 | `apps/main-app/src/views/capacity-allocation/allocation-results/components/tab-container/index.vue` | 删除 `:fullscreen` / `:-webkit-full-screen` / `::backdrop` 伪类规则，仅保留 `.result-content.is-css-fullscreen` |
| 4 | `apps/main-app/src/views/demand-forecast/version-management/components/modals/EditCollaborateVersionModal.vue` | 删除 `:fullscreen` / `:-webkit-full-screen` / `::backdrop` 伪类规则，仅保留 `.form-section--tabs.is-css-fullscreen` |

## Non-goals（未触及）

- 未修改全局 `.is-css-fullscreen` 样式定义
- 未修改 `usePlatformFullscreen` 公开 API 签名
- 未修改 `DecisionFullScreen` 通用组件（无需改动）
- 未修改 antdv-next 组件行为
- 未新增/删除任何依赖包

## Risk Mitigation

| Risk | 处理 |
|------|------|
| Select z-index 遮挡 | `getPopupContainer` 返回全屏容器自身（CSS 全屏 `overflow:auto` 可能裁剪，但 `.is-css-fullscreen` 的 `overflow:auto` 不影响 `position:absolute` 下拉挂载） |
| 产能分配兼容 | 已有 `resultContentRef.value` 返回，无需改动 |
| 通用组件 full-screen.vue | 无 target 参数时作用于 `document.documentElement`，预期正常 |
