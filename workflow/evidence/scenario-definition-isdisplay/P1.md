# P1 — 开发证据

> change: `scenario-definition-isdisplay` · mode: full

## Changed Files

| 文件 | 变更 |
|---|---|
| `apps/main-app/src/api/capacity-allocation/scenario.ts` | `ScenarioParamDefinition` 新增 `isDisplay?: boolean` |
| `apps/main-app/src/views/capacity-allocation/scenario-config/components/ParamCard.vue` | `sortedDefinitions` 先 filter `isDisplay !== false` 再排序 |
| `apps/main-app/src/views/capacity-allocation/version-management/components/CreateVersionModal/ParamGrid.vue` | `normalizeFields` definitions 先 filter `isDisplay !== false` 再 map |
| `apps/main-app/src/views/capacity-allocation/version-management/components/CreateVersionModal/composables/useCreateVersionModal.ts` | `initGroupValues` / `handleOk` 遍历 definitions 时 `if (def.isDisplay === false) continue;` |

## Non-goals 确认（未触及）

- ✅ `data.md` 未改
- ✅ `applyDetailTableValues`（库存/区域表格行，来自 paramValues 非渲染开关）未改
- ✅ `useBaseScenarioParamGroups.ts` / `InventoryLevelCard.vue` 未改

## 风险缓解落地

- Risk 1（渲染/提交一致性）：4 处同步过滤（渲染×2 + 初始化 + 提交），统一 `isDisplay !== false`
- Risk 2（isDisplay 缺失兼容）：判定 `!== false`，缺失/`true` 均渲染
- Risk 3（库存表格误伤）：未动 applyDetailTableValues
