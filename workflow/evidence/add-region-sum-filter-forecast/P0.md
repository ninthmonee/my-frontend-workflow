# Evidence: P0 — add-region-sum-filter-forecast

- change: add-region-sum-filter-forecast
- gateReady: YES
- Scope:
  - 需求预测 → 预测结果 tab（forecast-results/components/tab-container/index.vue，单文件）：
    - 新增 regionSumFilter 状态（ref<string[]>）与 regionFilter 平行
    - 新增 regionSumOptions 数据源，onMounted 从 enumStore.getCustomerRegionSumOptions() 填充（与 regionOptions 平行）
    - 模板 filter-bar 中「区域部门」之前插入「区域」Select field（mode=multiple, props 与区域部门 Select 一致）
    - params computed 预测结果分支新增 zone_sum: regionSumFilter.value（区域部门 zone 保持现有）
  - Non-goals（明确不做）:
    - 不改多版本对比/预测偏差比对/版本信息 tab
    - 不改 enum store / API / 其他页面
- Risks:
  1. 子应用契约风险：zone_sum 为新参数名，子应用需支持；zone 为已有参数不变——仅新增键不破坏现有
  2. 数据源时机：getCustomerRegionSumOptions 在 enum ensureLoaded 后才有数据；regionOptions 现有模式是 onMounted 直接读取（enum 通常已初始化），保持一致
  3. params computed 响应式：zone_sum 加入后随 regionSumFilter 变化自动 setData 子应用——确认 EmbedMicroApp watch immediate 已处理（经验库已验证）
