# Evidence: P0 — add-region-sum-filter-supply-network

- change: add-region-sum-filter-supply-network
- gateReady: YES
- Scope:
  - 供应网络（master-data/supply-network）3 文件：
    - types.ts：SupplyNetworkFilterValues 新增 `saleZoneSumIn: string[]`（区域多选）
    - useSupplyNetworkOrchestration.ts：filters 默认值 `saleZoneSumIn: []` + 查询 params 透传（模式同 saleZoneIn：空数组→undefined）
    - control-bar/index.vue：新增 saleZoneSumOpts computed（enumStore.customerRegionSumFilterOptions）+ 「区域」field 插入在「区域部门」之前（v-model 绑 localFilters.saleZoneSumIn）
  - 数据源复用 enum saleZoneSum options（已存在）
  - Non-goals（明确不做）:
    - 不改其他页面 / enum store / API 函数
    - 不改 en-US 语言包
- Risks:
  1. 后端契约：saleZoneSumIn 参数名需后端支持（与 saleZoneIn 同形态数组）
  2. 类型完整性：SupplyNetworkFilterValues 新增必填字段 saleZoneSumIn 需同步 filters 默认值，否则 undefined 访问报错——2 处同步
  3. UI 顺序：「区域」必须在「区域部门」之前
