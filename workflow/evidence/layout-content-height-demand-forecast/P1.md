# P1 — 开发

- gateReady: YES

## Changed files

| # | 文件 | 改动 |
|:---|:---|:---|
| 1 | `.../forecast-results/index.vue` | `.page-shell__body` −2 行（`flex:1`, `min-height:0`） |
| 2 | `.../version-management/index.vue` | `.page-shell__body` −2 行 |
| 3 | `.../tab-container/index.vue` | `.tab-shell` −3 行（`height:100%`, `min-height:0`, `overflow:hidden`）; `.tab-content` −2 行; `.tab-content :deep()` −2 行; `.tab-scroll` −3 行; `.predict-content` −3 行 |
| 4 | `.../version-info.vue` | `.version-info` −3 行（`flex`, `min-height`, `overflow`） |
| 5 | `.../version-table/index.vue` | `.table-card` −3 行（`height:100%`, `min-height:0`, `overflow:hidden`）; `.table-card__body` −2 行 |

**合计：5 files，−23 行，仅删除，无新增逻辑**

### 文件清单（`git diff --name-only`）

```
apps/main-app/src/views/demand-forecast/forecast-results/index.vue
apps/main-app/src/views/demand-forecast/forecast-results/components/tab-container/index.vue
apps/main-app/src/views/demand-forecast/forecast-results/components/tab-container/version-info.vue
apps/main-app/src/views/demand-forecast/version-management/index.vue
apps/main-app/src/views/demand-forecast/version-management/components/version-table/index.vue
```

## Non-goals respected

- ✅ 页面根容器和 `.page-shell` 保留原有 `height: 100%` + `overflow: auto`
- ✅ 其他模块（产能分配、订单分级等）未改动
