# P4 — supply-network-modal-refactor

**status**: done
**knowledgeDone**: YES
**precipitated**: YES

## 沉淀条目

- **文件**: `experiences/modal.md` — 新增「声明式 v-model:open → vue-modal-provider 命令式迁移」条目
- **severity**: high
- **tags**: `modal, vue-modal-provider, useModalRef, refactor`
- **核心内容**: 
  1. Modal 组件从 `defineModel('open')` + `emit` → `useModalRef()` + `resolve()` 
  2. 父组件从 `v-model:open` + ref 状态 → `useModal().show(args)` 命令式
  3. destroy-on-close 模式下无需 `watch(visible)` 重置 — 组件销毁后重建自然干净
- **INDEX.md**: experiences.md 中 Modal 条目数 3 → 4
