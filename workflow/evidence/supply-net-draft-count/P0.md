# P0 — supply-net-draft-count

## Scope

**涉及文件（2 个）：**

| 文件 | 变更类型 | 说明 |
|:-----|:---------|:-----|
| `apps/main-app/src/api/master-data/supply-network.ts` | 新增 API | `getSupplyNetDraftCountApi(versionId)` → `GET /app/rest/supply-net/{versionId}/draft/count` |
| `apps/main-app/src/views/master-data/supply-network/useSupplyNetworkOrchestration.ts` | 重构 | `totalPathsFromVersion`: computed → ref；新增 `refreshTotalPaths()` 按模式调不同接口 |

**非目标（明确不做）：**
- 不修改 ControlBar 组件（props 接口不变）
- 不修改 index.vue（传递的 prop 名不变）
- 不修改其他编辑模式 API（lock/unlock/search 不变）

## Risks

| # | 风险 | 缓解 |
|---|------|------|
| 1 | 编辑模式下新增/删除路径后 count 可能未及时刷新 | `handleDeleteDetail` / `handleAddPath` 末尾调用 `refreshTotalPaths()` |
| 2 | `totalPathsFromVersion` 从 computed 改 ref 后，初始化时序需确保版本列表加载完再设置初始值 | 在 `loadVersionList()` 末尾设置初始值 |
