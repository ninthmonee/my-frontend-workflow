# P1 — target-monitoring

## Changed files

| 文件 | 变更说明 |
|------|---------|
| `apps/main-app/src/views/tracking-monitoring/target-monitoring/index.vue` | 从 ComingSoon 占位重写为完整看板页面 |

## Non-goals respected

- ✅ 未新增子组件（单文件即完整页面）
- ✅ 未修改路由配置（已存在）
- ✅ 未修改菜单/i18n（已存在）
- ✅ 未新增 API 调用（全部从 useEnumStore 获取）
- ✅ 未改写法：使用 ref（非 reactive）、computed 派生状态、EmbedMicroApp 集成模式、CSS 变量主题系

## 实现要点

1. **Layout**：三层结构 — header（title + tooltip + subtitle）→ filter-bar（8 个 Select）→ tab-card（EmbedMicroApp）
2. **枚举**：8 个筛选项全部通过 useEnumStore 获取，产品级别额外保留排序（STAR→COW→SKINNY_DOG→SEED）
3. **子应用**：使用 EmbedMicroApp（name=target-monitoring, path=/workspace/embed/2067207235448086528），筛选参数通过 params computed 实时传递
4. **样式**：完全复用 warning-management 的 CSS 变量体系与布局模式
