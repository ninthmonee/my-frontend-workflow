# Evidence: P0 — wire-salezone-sum-to-api

- change: wire-salezone-sum-to-api
- gateReady: YES
- Scope:
  - 客户分级 useCustomerOrchestration.ts：getCommonParams 透传 saleZoneSum、toPostParams 逗号→数组、defaultParams 初始值 ''、watch 筛选变化数组加入 filters.value.saleZoneSum
  - 产品分级 useProductOrchestration.ts：同上 4 处
  - 与 saleZones（区域部门）完全平行；UI/类型层（type.ts + control-bar）上一轮已完成，本轮不动
  - Non-goals（明确不做）:
    - 不改 type.ts / control-bar / enum store / API 函数
    - 不改 en-US 语言包
- Risks:
  1. 后端契约风险：saleZoneSum 参数名需后端支持（API 层 params: any 无类型约束，后端不识别会忽略或报错）——已在交付说明中标注需后端配合
  2. watch 遗漏风险：若 watch 触发列表未加 saleZoneSum，筛选变化不触发请求——通过 grep 4 处接入点核对
  3. 类型安全：defaultParams 未加 saleZoneSum 会导致 filters 初始值缺失（可选字段不报错但行为不一致）——与 saleZones 对齐
