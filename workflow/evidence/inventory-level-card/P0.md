# P0 证据 — inventory-level-card

## Scope

### 涉及文件

| 文件 | 变更 |
|------|------|
| `apps/main-app/src/views/capacity-allocation/scenario-config/constants.ts` | 新增 `DOMESTIC_STOCKING_EXPECTATION_UB` 参数组（code: `domestic_stocking_expectation_ub`）；扩展 `ScenarioParamGroup` 接口 |
| `apps/main-app/src/views/capacity-allocation/scenario-config/components/InventoryLevelCard.vue` | **新建** — 库存水位参数卡片组件 |
| `apps/main-app/src/views/capacity-allocation/scenario-config/index.vue` | cards-grid 循环中条件渲染 InventoryLevelCard |
| `apps/main-app/src/views/capacity-allocation/scenario-config/composables/useScenarioConfig.ts` | 新增 `inventoryLevelData` ref 及编辑态管理 |

### 非目标（明确不做）

- ❌ 不新增/修改 API 调用 — `saveEdit()` 已有 TODO，库存水位沿用相同模式
- ❌ 不修改 ParamCard.vue — 新组件独立实现
- ❌ 不修改 i18n 文件
- ❌ 不修改场景创建/删除流程

## Risks

| # | 风险 | 影响 | 缓解 |
|---|------|------|------|
| 1 | `ScenarioParamGroup` 新增可选字段后，`ParamCard.vue` 的 `v-for="(item, idx) in group.items"` 可能因 `items` 存在于新组定义中而产生意外渲染 | 低 — `items` 仍保留，新组通过额外字段 `locations`/`products` 在 InventoryLevelCard 中使用 | 新组仍提供空的 `items: []`，不影响 ParamCard |
| 2 | 编辑态库存水位数据在 composable 中管理，切换场景时需正确重置 | 中 — 切换场景可能残留上一条数据 | watch activeKey 时同时重置 `inventoryLevelData` |
