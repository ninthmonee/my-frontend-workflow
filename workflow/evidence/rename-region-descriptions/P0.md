# Evidence: P0 — rename-region-descriptions

- change: rename-region-descriptions
- gateReady: YES
- Scope:
  - 将 `apps/main-app/src` 下 **36 个文件**（30 源文件 vue/ts/tsx + 4 .md 文档 + 1 locales/zh-CN/page.json + 1 api 注释）中所有中文「大区」字样替换为「区域部门」
  - 覆盖模块：store/enum.ts、api/master-data/supply-network.ts、locales、capacity-allocation、demand-forecast、master-data/supply-network、order-grading、tracking-monitoring
  - 衍生词（各大区/该大区/请选择大区/销售大区/大区分布/大区多选/按大区/5 大区/单个大区）随直接替换变为「各区域部门」等，语义保持
  - Non-goals（明确不做）:
    - 不改英文标识符/API 契约字段：saleZone、saleZoneIn、customerRegion、region、regionLabel 等
    - 不改 en-US 语言包（无中文「大区」）
    - 不改 workflow/evidence/ 历史证据文件
    - 不改任何程序逻辑字符串（option value、接口参数、message key 等——经审查这些位置均无「大区」字样）
- Risks:
  1. UI 布局风险：表格列标题「大区」→「区域部门」（2 字→4 字），固定列宽（如 120px）下可能换行/截断，需抽查 supply-table/TargetDeviationTab/ThresholdConfigTab 等列定义
  2. 替换误伤风险：批量替换可能触碰注释中的英文或特殊上下文，需逐文件 git diff 审查确认仅中文文本变化
  3. 契约一致性风险：若任何「大区」出现在与后端交互的字符串（如占位符/枚举值）中，替换会破坏契约——已初步确认无，但需复核
