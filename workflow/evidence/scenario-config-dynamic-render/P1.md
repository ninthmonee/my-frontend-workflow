# P1 — 开发证据

> change: `scenario-config-dynamic-render` · mode: full

## Changed Files

| 文件 | 变更 |
|---|---|
| `apps/main-app/src/api/capacity-allocation/scenario.ts` | 新增 `ScenarioParamDefinition` / `ScenarioDetailGroup` 类型；`ScenarioDetail.groups` 改为 `ScenarioDetailGroup[]`（+name/+definitions） |
| `apps/main-app/src/views/capacity-allocation/scenario-config/composables/useScenarioConfig.ts` | 新增 `detailGroups` ref 保留完整 groups；`loadScenarioDetail` 写入；`saveEdit` 提交顺序基于 detailGroups（空回退 SCENARIO_GROUP_CODES）；return 暴露 |
| `apps/main-app/src/views/capacity-allocation/scenario-config/index.vue` | 移除 constants import；解构 detailGroups；卡片区按 detailGroups 动态渲染 + 按 groupCode 分流三种卡片；空态占位 |
| `apps/main-app/src/views/capacity-allocation/scenario-config/components/ParamCard.vue` | group prop → ScenarioDetailGroup；按 definitions(sortOrder) 渲染 name+description；空态占位；样式新增 `.param-card__empty` |
| `apps/main-app/src/views/capacity-allocation/scenario-config/components/InventoryLevelCard.vue` | group prop 类型更新为 ScenarioDetailGroup（标题取 group.name） |

## 需求变更记录（Phase 3 回退）

- 用户确认：**区域平衡不显示表格**（去掉大区/上限/下限），显示格式为 name + description。
- 处理：删除已新增的 `RegionalBalanceCard.vue`（表格实现，与 ParamCard 重复）；REGIONAL_BALANCE 与其他普通组统一走 `ParamCard`（name + description 动态渲染）。
- 影响面：仅组件层收敛，类型层（ScenarioDetailGroup）与数据流（detailGroups）不变。

## Non-goals 确认（未触及）

- ✅ `CreateScenarioModal.vue` 未改（仍用 constants）
- ✅ `version-management/*`（CreateVersionModal/ParamGrid/constants）未改
- ✅ `UpdateScenarioParams` 契约未改
- ✅ 未引入参数值编辑控件（保持只读展示 + 组勾选）
- ✅ `STOCKING_EXPECTATION` 表格内部结构未改
- ✅ `constants.ts` 的 `SCENARIO_PARAM_GROUPS` / `SCENARIO_GROUP_CODES` 保留（CreateScenarioModal 依赖）

## 用户决策记录

- 区域平衡「大区」列显示 `definition.name`（如「内销占比范围」），非 code。

## 风险缓解落地

- Risk 1（definitions 缺失）：ParamCard/RegionalBalanceCard 均有空态占位；标题用 `group.name`（接口无则 undefined 时显示空，与现状一致）
- Risk 2（REGIONAL_BALANCE 数值结构）：取值链 `displayValue.max/min` → `valueJson.defaultMax/defaultMin` → "—"
- Risk 3（saveEdit 组集合）：detailGroups 为空回退 SCENARIO_GROUP_CODES
- Risk 4（constants 误删）：未删 constants；index.vue 仅移除 import
