# P4 — 知识沉淀

Change: `currency-switch`

## 结果

- **沉淀**: `status: skipped`（用户确认跳过）
- **knowledgeDone**: YES
- 用户选择不沉淀本次经验

## 可沉淀候选（未写入，供后续参考）

- 币种切换组件（header Segmented + enum store 注册 + fetchCurrencies 默认选第一个）
- `X-Currency` header 注入（request.ts 拦截器运行时读取 store，避免循环依赖）
- `useCurrency` composable：`formatMoney`（formatNumberAutoUnit + basicUnit 拼接）、`columnTitle`（含/不含币种名双场景）
- 表格金额列动态化：`columnsForTable` computed 中按 key 映射覆盖 title/render，避免破坏 columnSettings 持久化
