# P0 — remove-redundant-watch-visible

## Scope

**涉及文件（9 个）：**

| # | 文件 | watch 内容 | 类型 |
|---|------|-----------|------|
| 1 | `capacity-allocation/.../CreateVersionModal/index.vue` | `resetOnOpen()` | 简单重置 |
| 2 | `scenario-config/.../CreateScenarioModal.vue` | `resetForm()` + `clearValidate()` | 简单重置 |
| 3 | `demand-forecast/.../CreateAlgoRunVersionModal.vue` | `resetForm()` + `clearValidate()` | 简单重置 |
| 4 | `demand-forecast/.../AdjustTraceModal.vue` | `loadData()` | 简单加载 |
| 5 | `demand-forecast/.../EditCollaborateVersionModal.vue` | 3 async + `doLock()` | 重型加载 |
| 6 | `demand-forecast/.../CreateManualAdjustVersionModal.vue` | 3 async + `clearValidate()` | 重型加载 |
| 7 | `demand-forecast/.../modification-drawer/index.vue` | `loadList()` + 额外 watch | 双 watch |
| 8 | `order-grading/product/.../diff-analysis/index.vue` | 触发重拉 | 数据加载 |
| 9 | `order-grading/customer/.../diff-analysis/index.vue` | 触发重拉 | 数据加载 |

**非目标：**
- 不修改已有的业务逻辑（只删除 watch 并迁移到 onMounted）
- 不修改任何 API 调用、状态管理、模板结构
- 不修改 `{ immediate: true }` 以外未涉及的代码路径

## Risks

| # | 风险 | 缓解 |
|---|------|------|
| 1 | 移除 watch 后首次打开弹窗可能不触发初始化 | 每个文件逐一验证 `onMounted` 是否正确替代了 watch 回调节奏 |
| 2 | `modification-drawer` 有额外的 `watch(versionId)` 逻辑，需单独处理 | 仅移除 `watch(visible)`，保留 `watch(versionId)` |
