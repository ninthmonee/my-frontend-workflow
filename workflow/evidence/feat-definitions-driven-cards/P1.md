# P1 — feat-definitions-driven-cards

## Changed Files

| 文件 | 改动 |
|------|------|
| `apps/main-app/src/api/capacity-allocation/scenario.ts` | 新增 `ScenarioParamDefinition` 类型；`ScenarioDetail.groups` 增加 `name` (required)、`definitions` 字段；`paramValues` 类型放宽 |
| `apps/main-app/src/views/capacity-allocation/scenario-config/composables/useScenarioConfig.ts` | 新增 `scenarioDetail` ref，存储完整 API 详情并暴露给 UI |
| `apps/main-app/src/views/capacity-allocation/scenario-config/index.vue` | 新增 `sortedGroups` computed 从 `scenarioDetail` 按序排列；卡片渲染数据源从 `SCENARIO_PARAM_GROUPS` 硬编码 → API 的 `groups[].definitions` + `groups[].name` |
| `apps/main-app/src/views/capacity-allocation/scenario-config/components/ParamCard.vue` | props 从 `group: ScenarioParamGroup` → `definitions: ScenarioParamDefinition[]` + `groupName: string`；渲染 `def.name` + `def.description` |
| `apps/main-app/src/views/capacity-allocation/scenario-config/components/InventoryLevelCard.vue` | props 同上；新增 `definitionMap` computed 按 `${loc}_${prod}` 索引 definitions；表格单元格显示 `displayValue` + unit + `description` |

## Non-goals 确认

- ✅ 未触及 CreateScenarioModal / CreateVersionModal
- ✅ 未改变编辑模式交互（复选框、保存/取消）
- ✅ 未改变场景创建/删除逻辑
- ✅ 未改变 CSS 样式（仅 InventoryLevelCard 新增 `.stocking-cell-value` / `.stocking-cell-desc`）
- ✅ 保留 `SCENARIO_PARAM_GROUPS` 在 constants.ts 供 CreateScenarioModal 使用
