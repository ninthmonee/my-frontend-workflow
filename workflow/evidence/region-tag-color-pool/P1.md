# Evidence: P1

- runId: region-tag-color-pool

## Changed Files
- apps/main-app/src/store/enum.ts
- apps/main-app/src/views/order-grading/customer/components/charts-grid/index.vue
- apps/main-app/src/views/tracking-monitoring/warning-management/components/TargetDeviationTab.vue
- apps/main-app/src/views/tracking-monitoring/warning-management/components/ThresholdConfigTab.vue

## Key Decisions
- 移除 `ENUM_FALLBACK_MAP` 中 CustomerRegion 兜底（写死 5 大区：内销/欧洲/北美/亚太/日韩）与 `TAG_COLOR_MAP` 写死颜色映射
- 新增固定 50 色数组 `CUSTOMER_REGION_TAG_COLORS`（antd 色板深色系，白字可读），`fetchCustomerRegionOptions` 按接口返回顺序 `index % 50` 绑定 `tagColor`
- 预警管理 `TargetDeviationTab` / `ThresholdConfigTab` 的 saleZone Tag 颜色改走 `enumStore.getCustomerRegionTagColor(v)`（统一出口），移除各自写死 5 大区颜色映射
- 非目标确认未触及：`DimensionEditModal` 大区数据源（仍读 meta-data 注册表）、`StrategicLevel`/`ProductLevel` 的 ENUM_FALLBACK_MAP 兜底、customerLevel/productLevel 的 tagColorMap
- 注：Changed Files 中的 `charts-grid/index.vue` 为上一轮（chart-region-amount-convert）未提交改动，不在本次范围

## Gate
- gateReady: YES
