# P4 — 知识沉淀

Change: `currency-refetch`

## 结果

- **沉淀**: `status: skipped`（用户确认跳过）
- **knowledgeDone**: YES

## 可沉淀候选（未写入，供后续参考）

- **lastRequestedCurrency 模式**：页面编排中记录「上次请求时的全局偏好值」（如币种），watch 变化时仅当与上次不同才重新请求——区分「初始化赋值（null→默认）」与「用户主动切换」，避免首次进入页面重复请求
- **全局偏好切换（币种/语言等）标准三步**：① store 持久化（localStorage）② request 拦截器注入 header ③ 各页面 orchestration watch 联动重新请求当前数据（保留筛选/版本/分页，编辑态先退出）
