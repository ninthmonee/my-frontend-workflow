# P1 — 开发

- gateReady: YES

## Changed files

### 新增（1 file）

| 文件 | 说明 |
|:---|:---|
| `packages/@core/composables/src/use-platform-fullscreen.ts` | iOS 降级全屏 composable：isIPadOSSafari() 检测 + CSS 降级路径 + 原生委托 |

### 修改（7 files）

| 文件 | 改动简述 |
|:---|:---|
| `packages/@core/composables/src/index.ts` | +1 行，导出 `use-platform-fullscreen` |
| `packages/@core/base/design/src/css/global.css` | +12 行，`.is-css-fullscreen` 全局 class（fixed + z-index:9999） |
| `packages/@core/ui-kit/shadcn-ui/.../full-screen.vue` | −12 行，`useFullscreen` → `usePlatformFullscreen`，移除手动状态同步 |
| `apps/.../forecast-results/.../tab-container/index.vue` | −20 行，`toggleFullscreen` + 事件监听 → `usePlatformFullscreen(predictContentRef)` |
| `apps/.../allocation-results/.../tab-container/index.vue` | −20 行，`toggleFullscreen` + 事件监听 → `usePlatformFullscreen(resultContentRef)` |
| `apps/.../EditCollaborateVersionModal.vue` | −22 行，`toggleFullscreen` + 事件监听 + `onMounted/onUnmounted` → `usePlatformFullscreen(tabContentRef)` |
| 3 个页面级 CSS | `:fullscreen` 伪类增加 `.is-css-fullscreen` class 并列选择器 |

### 文件清单（`git diff --name-only`）

```
apps/main-app/src/views/capacity-allocation/allocation-results/components/tab-container/index.vue
apps/main-app/src/views/demand-forecast/forecast-results/components/tab-container/index.vue
apps/main-app/src/views/demand-forecast/version-management/components/modals/EditCollaborateVersionModal.vue
packages/@core/base/design/src/css/global.css
packages/@core/composables/src/index.ts
packages/@core/ui-kit/shadcn-ui/src/components/full-screen/full-screen.vue
```

## Non-goals respected

- ✅ 未修改 `modal.vue`（已在用 CSS 模拟全屏）
- ✅ 未修改 `header.vue`（仅使用 DecisionFullScreen，无需改）
- ✅ 未引入新第三方依赖
- ✅ 不影响桌面 Chrome/Safari / Android Chrome 全屏行为
- ✅ 未修改 `@vueuse/core`

## 改动量统计

- 新增：1 file (101 lines)
- 修改：6 files (+13 / −79 lines)
- 净减：−66 lines
