# Evidence: P0 — add-demand-fulfillment-tab

- change: add-demand-fulfillment-tab
- gateReady: YES
- Scope:
  - 产能分配 → 产能分配结果页 tab-container（单文件）新增「需求满足」看板 tab：
    - 看板 ID: 2085294974701674496（嵌入路径 /workspace/embed/<id>）
    - items 数组插入位置：产能分配结果之后、销售目标之前
    - 独立筛选栏（不复用 common* 共享筛选）：区域(zone_sum)、区域部门(zone)、客户分级(customer_level_code)、材料(material)、产品分级(product_level_code)、手套产品(gloves_code)、利润档位(profit_level_code)
    - params: version_ids=[props.versionId] + request_ids=[props.requestNum] + 上述 7 个筛选
    - 数据源复用已有：enumStore.customerRegionSumFilterOptions（区域）、getMaterialOptions、glovesNameOptions、customerLevelOptions/productLevelOptions/profitLevelOptions（onMounted 已填充）、regionOptions
    - subAppHeight 新增横屏/竖屏高度 case；EmbedMicroApp name="demand-fulfillment"（区别于 allocation-results）
  - Non-goals（明确不做）:
    - 不改现有 tab（产能分配结果/销售目标/利润参考/内销库存计划/多版本对比/基础信息）的共享筛选逻辑与参数
    - 不改 enum store / API / 其他页面
    - 不新增后端接口
- Risks:
  1. 子应用契约风险：9 个参数名（version_ids/request_ids/zone_sum/zone/customer_level_code/material/product_level_code/gloves_code/profit_level_code）需子应用支持——其中 zone_sum 为新参数（需求预测已用同模式），其余与现有 tab 参数一致
  2. EmbedMicroApp name 冲突：name="demand-fulfillment" 必须区别于现有 "allocation-results"，否则 setData 串台——独立 name 规避
  3. 高度估算风险：9 参数筛选栏可能多行换行，subAppHeight 固定高度需按实际行数估算（横屏 1400px/竖屏 1800px 基线，可微调）
  4. params 响应式：新增分支后随 demand* ref 变化自动 setData（EmbedMicroApp watch immediate 已验证）
