# Evidence: P0 — rename-salezone-sum-param

- change: rename-salezone-sum-param
- gateReady: YES
- Scope:
  - 客户分级 + 产品分级 6 文件：
    - type.ts：saleZoneSum 字段改为 saleZoneSums（string[]，数组）
    - control-bar：区域 field 的 max-tag-count 1→0（产品已是 0）；v-model 改绑数组（不再逗号串 computed）
    - orchestration：参数名 saleZoneSum → saleZoneSums；GET 请求直接传数组（移除 toPostParams 的逗号 split）；defaultParams/watch 同步
  - Non-goals（明确不做）:
    - 不改 enum store / API 函数 / 表格
    - 不改其他页面（需求预测/产能分配/供应网络/目标监控仍用各自传参）
- Risks:
  1. 后端契约：参数名改为 saleZoneSums（复数）且为数组，需后端支持
  2. 类型完整性：saleZoneSum → saleZoneSums 需同步 type.ts/control-bar/orchestration 全部引用，漏改编译报错或行为错乱——grep 全量核对
  3. computed 移除：saleZoneSumArr 逗号串 computed 不再需要，改为直接 v-model 数组，避免残留
