# P0 证据 — own-brand-filter

## Scope

### 涉及文件
| # | 文件 | 改动 |
|---|---|---|
| 1 | `apps/main-app/src/api/demand-forecast/version-management.ts` | `PageForecastResultItem` + `ownBrand: string`; `PageForecastResultParams` + `ownBrand?: string[]`; `ForecastBatchAdjustPayload` + `ownBrand?: string[]` |
| 2 | `apps/main-app/src/views/demand-forecast/version-management/composables/useEditCollaborateData.ts` | filterState、loadPageForecastResult、handleApplyCoefficient、buildEditableRow、editableColumns 各加 ownBrand |
| 3 | `apps/main-app/src/views/demand-forecast/version-management/components/modals/components/DirectEditTabContent.vue` | filterState defineModel 类型 + 筛选行新增 Select |
| 4 | `apps/main-app/src/views/demand-forecast/version-management/components/modals/EditCollaborateVersionModal.vue` | 兼容性确认（预期无需改动） |

### 非目标（明确不做）
- 不修改版本管理列表页（version-table）的任何列或筛选
- 不修改 UploadTabContent（上传 Tab）
- 不修改 CreateAlgoRunVersionModal / CreateManualAdjustVersionModal
- 不修改其他模块（产能分配-版本管理、预测结果页面）
- 不新增国际化 key（options 内联硬编码）

## Risks

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| 1 | 后端 API 尚未返回 `ownBrand` 字段 → `item.ownBrand` 为 `undefined`，表格列显示 `-`，不会报错 | 低 | `buildEditableRow` 使用 `item.ownBrand ?? '-'` 兜底，Tag render 对非法值输出 `-` |
| 2 | `ownBrand` 字段类型为 `string` 但后端可能返回 `number`（`0`/`1`）→ Tag render 使用 `String()` 转换后比较 | 低 | render 中使用 `String(row.ownBrand)` 统一转字符串后判断 |
| 3 | 筛选参数 `ownBrand` 后端可能接受 `string` 而非 `string[]` → 取决于后端 API 约定 | 中 | 当前多选用 `string[]`，如后端要求单值则在 loadPageForecastResult 中 join |
